<div class="container mt-5">
    <div class="row">
        {{#each products}}
        <div class="col-md-3">
            <div class="card">
                <img class="mx-auto d-block" style="width: auto;height: 9rem;" src="{{this.image}}" class="card-img-top"
                    alt="Fissure in Sandstone" />
                <div class="card-body">
                    <h5 class="card-title">{{this.name}}</h5>
                    <p class="card-text">
                        {{this.description}}
                    </p>
                    <a href="#!" class="btn btn-primary">Buy Now</a>
                </div>
            </div>
        </div>
        {{/each}}
    </div>
</div>